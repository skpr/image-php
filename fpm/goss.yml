command:
  check-configuration:
    exec: "mkdir /data/app && php-fpm --test"
    exit-status: 0
    stdout:
      not:
        contain-substring: "WARNING"

  check-php-compass-extension:
    exec: "php -m | grep -i compass"
    exit-status: 0
    stdout:
      contain-substring: "compass"
